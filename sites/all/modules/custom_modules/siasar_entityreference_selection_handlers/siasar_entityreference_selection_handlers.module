<?php

/**
 * Implements hook_ctools_plugin_directory().
 */
function siasar_entityreference_selection_handlers_ctools_plugin_directory($module, $plugin) {
  if ($module == 'entityreference') {
    return 'plugins/entityreference/' . $plugin;
  }
}
